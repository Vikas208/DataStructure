#include<iostream>

using namespace std;
void trappint(int arr[],int n){
    
    int trap=0;
    for(int i=0;i<n;++i){
         
    }
}
int main(){

    int arr[] = {3,0,2,0,4};
    int n = sizeof(arr)/sizeof(arr[0]);
    trapping(arr,n);
    return 0;
}